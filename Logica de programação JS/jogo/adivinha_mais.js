function compare() {
    var segredo = 5
    var numPlayer = document.querySelector("input.numero").value

    if (numPlayer == segredo) {
        alert("Você acertou")
    }
    else{
        alert("Você errou")
    }
    
}